# zizvideo 本轮迭代（13 项）

> 用户 2026-09-20 点名。原则：面板模块、**无构建步骤**（原生 ESM）、文案 ≤40 字、
> **不许谎报**（做不到就如实标"不支持/未实现"）。发布由父 agent 统一做，代理不要 bump/release。

## 分工（文件域不重叠，别碰别人的文件）

| 波次 | 条目 | 主文件 |
|---|---|---|
| A 播放器与播放策略 | 1 2 3 4 5 6 7 13 | `assets/js/feed.js`、`assets/js/api.js`、`internal/api/handlers_feed.go`、`internal/storage/**` |
| B 剧场 + 底栏 + 收藏 | 9 10 12 | `assets/js/{admin.js,app.js,feed.js 之外的页面}`、新增页面 JS、`internal/api/handlers_series.go`、`internal/api/handlers_me.go`、`internal/storage/**` |
| C 管理端 | 8 11 | `assets/js/admin.js`、`internal/api/handlers_admin.go`、`internal/config/config.go`、`internal/storage/**` |

⚠️ A 与 B 都会碰 `feed.js` 的**底栏/入口**区域：**A 只管播放器与进度条、B 只管底栏挂载点**，
B 不要改 `feed.js` 里的播放逻辑，A 不要动底栏容器；两边各自加自己的 DOM 容器与事件。

## 逐条要求

**1 静音按钮**：单击画面不再切静音；改成画面角落一个 🔊/🔇 按钮，点它切静音（记住用户选择，localStorage）。
**2 单击画面 = 暂停/继续**：单击画面切换播放/暂停，显示中央 ▶/⏸ 提示（300ms 淡出）。
**3 进度可拖动**：进度条可点击/拖动 seek（鼠标 + 触摸），拖动时显示时间气泡；松手后 `currentTime` 生效并**立刻 PATCH 进度**。
**4 默认随机播放**：进入播放页默认即为随机顺序（见 6 的随机实现）。
**5 上滑过渡 + 跳两个的 bug**：
   - 过渡生硬 → 用 CSS transform 做位移动画（`transition: transform .28s ease-out`），禁止用 scroll-snap 硬跳；
   - **短内容一次跳两条**根因多半是"滚轮/触摸事件在一次手势里触发了多次切换" → 加**手势锁**（一次手势只允许切一条，`isAnimating` 或时间阈值 400ms），并保证 `IntersectionObserver` 的回调不叠加。**必须写门禁**覆盖"一次手势只前进一条"。
**6 无限滑动 + 播完前不重复**：随机用 **seed + hash 排序**实现（`ORDER BY hash(id||seed)`），每用户持久化 `feed_seed` 与已播计数；**未播完不重复**；全部播完后换新 seed 进入下一轮。翻页游标沿用现有 `next_cursor`。
**7 循环与自动播设置（二级菜单）**：播放页右上放 ⚙ 图标 → 弹出二级设置：`循环播放`（**仅在非连播/非自动播时生效**）、`自动播放下一个`（默认开）。设置持久化到后端（每用户）；同面板加 `左右键跳转秒数`（默认 10，1–120），左右键前后跳转，剧场同样生效。
**8 管理员可开关注册**：配置项 `allow_register`（默认**关**）；关时 `/api/v1/setup|register` 之外的注册入口一律 404/403 并如实说明；管理页给开关 + 生效值回读。
**9 剧场（短剧）**：新增实体 `series`（id/标题/简介/封面/库归属/排序）+ `series_media`（剧集顺序）。
   播放逻辑：剧场页列出剧集 → 点进某剧 → **按序连播**（自动下一集、可手动选集）、显示"第 N 集"、返回键回到剧场列表；
   剧集内容**引用已有 media**（不复制文件、不新上传）；管理端可"新建剧场 + 把媒体加入某剧场并排序"。
   **必须写清设计**（表结构、接口、连播状态机、与 6 的随机不冲突：剧场内是顺序，不吃随机 bag）。
**10 底栏**：播放页底部四入口「首页（当前播放页）/剧场/收藏/我的」；「收藏」页三个 Tab：**点赞 / 收藏 / 历史**，
   每个 Tab 都可**清除记录**（二次确认，后端 DELETE 接口，如实报清了多少条）；「我的」= 账号、设置入口、退出登录。
**11 媒体去重（管理员）**：判据先用 `(size_bytes, duration_ms)` 分组（同组疑似重复），列出组内成员（路径/库/时长/加入时间）；
   默认只提供**删除面板记录**（不动磁盘文件），要删文件必须**单独勾选 + 手输确认**（危险动作，走任务中心 + 实时进度）。
   **绝不自动删**；检测结果要标明判据，别声称"内容完全相同"。
**12 管理页返回播放页入口**：管理页顶部固定一个「返回播放」按钮（返回首页/播放页）；键盘 Esc 也返回。
**13 播放页显示所属媒体库**：视频信息区显示"来自 <库名>"，**点击**把 feed 范围切到该库（并给"全部库"的返回入口）。

## 门禁与验证（每条都要能抓复发）
- 后端：随机不重复（同一 seed 内不重复、播完换 seed）、剧场顺序连播、去重检测只认记录不删文件、注册开关关时注册被拒。
- 前端：`node tools/check-js-syntax.mjs zizvideo/internal/web/assets`、`node tools/check-js-undeclared.mjs …`；
  手势锁"一次手势只切一条"（可用纯函数单测 + 浏览器验证）。
- **真机**：临时 data + 空闲端口起实例，用真视频（`scripts/gen-fixtures.sh` → `/tmp`）走一遍：
  1/2/3 交互、5 上滑只前进一条、6 随机播完前不重复、7 设置生效、9 剧场连播、10 底栏与清除记录、13 库范围切换；
  管理端 8/11/12 也要在浏览器点一遍。**不碰外接盘上的任何目录、不碰 7766 正在跑的服务、不重启本机**。

## 硬约束
不改 `internal/**` 之外的仓库代码；不 `git commit`；不 bump/release/publish；不改 `docs/坑清单.md`、`AGENTS.md`、
`ZizPanel-当前状态.md`；不碰 macsaber/MariaDB/外接盘；报告里列出**未实现/未验证**项。

---

## 补丁 R1：剧集序列识别（用户 2026-09-20 追加）

**目标**：把媒体加入剧场时（或"自动识别"按钮）从**文件名/路径**推导季与集号，**按序显示并连播**；识别不到的不许瞎猜。

**解析优先级**（先命中先算，全部大小写不敏感）：
1. `SxxExx` / `Sxx.EPxx`（季+集）
2. `EP?xx` / `E?xx`（集号；注意与 `1080p`/`720p`/年份区分）
3. `第xx集` / `第xx話/话`（支持中文数字：一/二/…/十/十一…）
4. 独立的"尾部数字"（形如 `剧名 - 03.mp4`、`剧名 03.mp4`、`剧名.03.mp4`），
   但**必须排除**：分辨率（`480|576|720|1080|1440|2160` + `p|i`）、年份（`19xx|20xx` 且后面跟分辨率/编码标记）、
   编码标记（`x264|x265|h264|h265|hevc|av1|aac|dts`）、`CD1/CD2` 之外的无关数字。

**排序**：先季（缺省 1），再集号，再文件名自然序（`02` < `10`）；**识别不到的排在末尾**并按文件名排，
界面上如实标「未识别（按文件名排）」——**不许把识别不到的塞进中间冒充有序**。

**接口/字段**：`series_media` 增加 `season`/`episode`（可空，空=未识别）与 `episode_source`
（`filename`|`manual`）；返回里带 `episode_label`（如「第 3 集」/「S1E3」/「未识别」）。
新增 `POST /api/v1/admin/series/{id}/detect`（按当前成员重新识别并返回**变化清单**，需 `confirm` 才落库）与
`PUT .../order` 保留（手动覆盖后 `episode_source=manual`，**自动识别不得覆盖 manual**）。

**门禁**：解析表（≥20 个真实命名：`S01E02`、`第03集`、`EP4`、`剧名 - 05.mp4`、`剧名.10.mp4`、
`Movie.2023.1080p.x265.mp4`（**不得**识别成 2023/1080）、`第 十 集`、`01.mp4`、`无编号.mp4`）；
排序（含未识别在末尾、自然序 2<10）；"自动识别不覆盖 manual"；接口 confirm 前后对比。
